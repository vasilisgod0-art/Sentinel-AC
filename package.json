{
  "name": "sentinel-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "postinstall": "npm install --prefix backend"
  },
  "engines": {
    "node": ">=18"
  }
}
